
	# https://docs.github.com/de/rest/emojis/emojis?apiVersion=2022-11-28

		# ..!???

	@ `emoji.php`:

		(a) tag INDEX plus search function

		(b) optional 3rd $SIZE parameter, so also my HTML output via shell, not only the URL.

